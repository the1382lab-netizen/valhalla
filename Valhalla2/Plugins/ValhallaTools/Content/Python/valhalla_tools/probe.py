"""Scratch module for one-off editor queries. Intentionally empty.

Phase 4c used this file to answer questions the documentation could not:
which Interchange pipeline properties exist in 5.8, what the imported meshes
actually measure, which way the socket bones point, how long each animation
is. Every answer that mattered is now a comment next to the code it explains,
or a named constant in ``ValhallaVisuals.h``.

It is left in place, empty, so the next investigation has somewhere to go that
is not a shipped module. Nothing imports it.
"""
